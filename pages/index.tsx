import type { NextPage } from "next";
import Layout from "../components/Layout";

export const name: string = "○○○○株式会社";

const Home: NextPage = () => {
	return (
		<Layout title="home">
	
		</Layout>
	);
};

export default Home;
